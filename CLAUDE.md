# 动效代码库 — 整理规范

## 项目说明

这是一个纯 HTML 动效资产库，部署于 GitHub Pages。`index.html` 是展示台，所有文件通过 iframe 预览，支持全屏查看和一键复制源码。

---

## 目录结构（当前方案）

```
动效代码库/
├── index.html                 ← 展示台，所有文件的入口，每次整理后必须同步更新
│
├── ── 动效技术库（01-09）──   按技术类型分类，可复用的动效片段
├── 01-CSS纯动效/
├── 02-Canvas数学驱动/
├── 03-SVG路径动画/
├── 04-滚动触发/
├── 05-粒子物理动效/
├── 06-WebGL-3D/
├── 07-UI组件/                 ← 功能型 UI 组件（播放器、弹窗、表单等）
├── 08-动效图表/               ← 数据驱动的动态图表，预留
├── 09-大屏可视化/             ← 3D 地图、数据大屏等，预留；JSON 数据文件与 HTML 放同目录
│
└── ── 完整案例库（10-12）──   完整品牌/主题设计参考，可拆解复用
├── 10-案例-Fig-Mint/          ← 虚构产品「Fig Mint」多页面网站，复古科技美学
├── 11-案例-Aura-Capital/      ← 虚构金融品牌「Aura Capital」，金融数据可视化
└── 12-个人主页模板/           ← 7种不同风格的个人主页/作品集模板
```

---

## 整理规则

### 收到新 HTML 文件时

1. **先判断归属**：是可复用动效片段（→ 01-09），还是完整页面/品牌案例（→ 10+）？
2. **能放进现有分类就直接放**，不要新建分类。只有当内容明显不属于任何现有分类时，才提出新分类方案并等待确认。
3. **文件命名规则**：`序号-语义名.html`，例如 `04-scroll-reveal.html`。序号在该文件夹内递增。
4. **同步更新 index.html**：在对应 section 的 `items` 数组里追加条目，格式：
   ```js
   { title: "中文标题", category: "文件夹名", url: "文件夹名/文件名.html", description: "一句话描述效果。" }
   ```

### 新增分类时

- 动效技术库新分类：编号接续（当前最大 09），放在 09 之后、完整案例库之前。
- 完整案例库新分类：编号接续（当前最大 12）。
- 在 index.html 的 `sections` 数组里同步新增对应 section 对象。

### 不要做的事

- 不删除任何文件。
- 不在未确认的情况下重构 index.html 的整体结构。
- 不因为"顺手"而重命名已有文件（会破坏 index.html 的 url 引用）。

---

## 重大调整流程

如果新内容与现有方案有冲突，或需要新增大类，先给出方案说明，等用户确认后再执行。不要自行决定。

---

## 技术说明

- 全部纯 HTML，无构建工具，无框架依赖。
- 部署目标：GitHub Pages（静态托管），JSON 数据文件可通过 fetch 正常读取。
- 本地预览需起本地服务器（`python -m http.server` 或 VS Code Live Server），直接双击打开会因 CORS 限制导致 JSON 无法加载。
